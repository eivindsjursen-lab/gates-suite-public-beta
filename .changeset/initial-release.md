---
"@gates-suite/core": major
"@gates-suite/cache-health-gate": major
---

Initial cache-first v1.0.0 release in gates-suite.

Includes Cache Health Gate and the shared core library (baseline engine, reason code registry, report rendering, and GitHub API client). Additional gates will be versioned in later releases.
